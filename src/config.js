import React from 'react';

/*
    Configuration panel for the application. This allows users to set global configuration parameters which take immediate effect.
*/

class Config extends React.Component {
    constructor(props) {
        super(props);
    }
}

export default Config;